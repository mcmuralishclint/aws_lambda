version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies"
      - pip install aws-sam-cli
  pre_build:
    commands:
      - echo "Cleaning up any failed stacks"
      - aws cloudformation delete-stack --stack-name hello-world-api || true
      - aws cloudformation wait stack-delete-complete --stack-name hello-world-api || true
  build:
    commands:
      - echo "Building application with SAM"
      - sam build
  post_build:
    commands:
      - echo "Packaging application with SAM"
      - sam package --s3-bucket hello-lambda-deployments-binodbandara --output-template-file packaged.yaml
      - echo "Deploying application with SAM"
      - |
        sam deploy \
          --template-file packaged.yaml \
          --stack-name hello-world-api \
          --region us-east-1 \
          --capabilities CAPABILITY_IAM \
          --no-fail-on-empty-changeset \
          --disable-rollback
artifacts:
  files:
    - packaged.yaml
